# WhatsApp Service - Production Environment Variables for Google Cloud Run
# Copy these to Cloud Run environment variables during deployment

# Server Port (Cloud Run sets this automatically)
# PORT=8080

# API Secret Key (IMPORTANT: Generate a strong random key for production)
API_SECRET_KEY=your-secure-random-api-key-here

# Firebase Configuration
FIREBASE_PROJECT_ID=eastern-crm-c57d9
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@eastern-crm-c57d9.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_FIREBASE_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# WhatsApp Session ID (unique identifier for production instance)
WHATSAPP_SESSION_ID=eastern-mills-production

# Frontend URL (for CORS - your production Netlify URL)
FRONTEND_URL=https://eastern-mills-crm.netlify.app

# Environment
NODE_ENV=production

# Webhook URL (optional - for AI processing of incoming messages)
WEBHOOK_URL=

# Production Notes:
# 1. API_SECRET_KEY: Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. FIREBASE_PRIVATE_KEY: Get from Firebase Console → Project Settings → Service Accounts
# 3. FRONTEND_URL: Must match your Netlify deployment URL
# 4. WHATSAPP_SESSION_ID: Use "eastern-mills-production" for prod (different from local/test)
